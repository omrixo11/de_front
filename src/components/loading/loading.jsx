import React from 'react';
import './loading.css';

const LoadingSpinner = () => {
    return (
        
        <div className="loading-container">
            <div className="loading-spinner">
                <img src="images/loading-icon-dessa.svg" alt="Loading Logo" />
            </div>
        </div>

    );
};

export default LoadingSpinner;
